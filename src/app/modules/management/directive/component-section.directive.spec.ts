import { ComponentSectionDirective } from './component-section.directive';

describe('ComponentSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentSectionDirective();
    expect(directive).toBeTruthy();
  });
});
